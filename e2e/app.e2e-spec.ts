import { InternPage } from './app.po';

describe('Intern Tracking System', () => {
  let page: InternPage;

  beforeEach(() => {
    page = new InternPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
